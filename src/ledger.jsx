import React from "react";
import ReactDOM from "react-dom/client";
import ConfigLedger from "./components/ConfigLedger.jsx";
import "./ledger.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ConfigLedger />
  </React.StrictMode>
);
